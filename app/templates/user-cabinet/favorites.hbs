<h2 class="selection-title-order">Бажані товари</h2>
	<div class="border-order"></div>

<div class="product-selection">
	{{#each model as |favorites|}}
		{{ui-favorite-product
			product=favorites.product
			id=favorites.id
			productId=product
		}}
	{{/each}}
</div>

<div class="favorites-btn-selection">
	{{#link-to "user-cabinet" class="favorites-btn"}}Приховати форму<i class="fas fa-sign-out-alt"></i>{{/link-to}}
</div>
{{outlet}}